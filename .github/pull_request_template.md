**What does this change?**

**Did you run it?**
- [ ] `maprot build --board sri-lanka` still renders
- [ ] `maprot doctor` passes

Small and focused beats big and sweeping. If you're changing how a place is
written up rather than how the code works, that belongs in `skills/maprot/SKILL.md`.
